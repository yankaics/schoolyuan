<!-----关注用户的表单---->

<%= form_for(current_user.active_relationships.build(followed_id: @user.id), remote: true) do |f| %> <!--remote: true为自动当Ajax处理--->
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "关注", class: "btn btn-primary" %>
<% end %>
